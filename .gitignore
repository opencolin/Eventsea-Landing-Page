node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.claude/*
# But version-control project-scoped skills so any agent on this repo
# has the canonical SOPs (event-leads, etc.) available.
!.claude/skills/
.env
.env.local